<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.temp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Temp">
        <activity android:name=".OrderStatus"></activity>
        <activity android:name=".RazorpayPayment" />
        <activity android:name=".PlaceOrder" />
        <activity android:name=".Dishes" />
        <activity android:name=".Hotels" />
        <activity android:name=".Vendors" />
        <activity android:name=".Services" />
        <activity android:name=".Foods" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".ChooseOption"
            android:label="@string/title_activity_choose_option"
            android:theme="@style/Theme.Temp.NoActionBar" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".PhoneAuth">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>